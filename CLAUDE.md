# 튜브폴더 (Tube Folder) 프로젝트

## 프로젝트 목표
유튜브 영상을 폴더로 정리하고 드래그앤드롭·정렬 기능을 제공하는 Chrome 확장 프로그램.

## 프로젝트 구조
- `tubefolder-extension-v1/` : 완성·배포된 버전1. 건드리지 않음(배포 유지 목적).
- `tubefolder-extension-v2/` : 버전2 개발 폴더. 설계·기술 스택 아직 미정.

## 기술 스택 (v1 기준)
- 언어: 순수 JavaScript (TypeScript 아님)
- 프레임워크: 없음 (React 등 프레임워크 미사용, 빌드 도구 없이 순수 HTML/CSS/JS)
- 배포: 폴더를 그대로 zip 압축 → Chrome 확장(Manifest V3) 또는 GitHub Pages(PWA)로 배포
- v2 기술 스택·프레임워크는 아직 결정되지 않음 — 착수 전 사용자 확인 필요

## 핵심 기능
- 유튜브 영상 폴더 관리
- 드래그앤드롭으로 영상 이동
- 폴더별 정렬 기능
- 유튜브 페이지 내 자연스러운 UI 통합

## 알려진 이슈
- (2026-07-19 확인) 과거 이 섹션에 있던 "MutationObserver 범위 과다·Shadow DOM 재귀 비용·6초 강제 리스캔" 문제와 `playlistAddButton.ts` 등 언급된 파일은 저장소 전체(git 히스토리 포함)에 존재하지 않음을 확인해 제거함. 다른 프로젝트 계획 당시 내용이 잘못 섞여 있던 것으로 추정.
- 실제 성능 문제가 재현되면 증상·재현 조건·수정 대상 파일을 이 섹션에 다시 기록할 것.

## 완전 자율 코딩 및 진화 원칙

### Claude Code 사용 권장
- 권장 대상: 파일 생성·수정, 버그 수정, 테스트 실행, 리팩토링 등 실제 코드를 다루는 모든 작업
- 권장 방식: 작업 전 "Claude Code에서 진행하시면 더 효율적입니다"라고 먼저 안내한다.
- 예외: 사용자가 이미 Claude Code에서 작업 중이거나 코드 설명·설계 논의인 경우 생략.

### 작업 시작 전 필수 절차
- 프로젝트 폴더 구조를 먼저 파악한 뒤 작업을 시작한다.
- 수정 대상 파일을 특정하고, 영향 범위를 먼저 분석한다.
- 테스트·리팩토링 전 반드시 현재 버전을 백업한다.
- 주요 변경 사유와 내용을 코드 주석 또는 커밋 메시지에 기록한다.

### 개발 체크리스트 작성 규칙
- 새 버전(v2 등) 또는 여러 기능이 묶인 작업을 시작할 때, 우선순위·진행 상황을 추적할 체크리스트 문서를 작성하고 항목 추가·완료 시마다 갱신한다.
- 문서 위치: 해당 버전 폴더 안에 `ROADMAP-CHECKLIST.md`로 둔다.
- 형식은 아래 4단 구성을 고정 틀로 유지한다.
  1. **단계 기준 표** — 각 단계(예: 1단계 구조 설계 / 2단계 핵심 기능 / 3단계 고급 기능 / 4단계 다듬기 / 5단계 출시 준비)의 기준 설명과 "N/M 완료" 진행률을 문서 맨 위에 둔다.
  2. **한눈에 보기 요약표** — 단계 | 항목 | 복잡도 | 상태(⬜ 미착수 / 🔧 진행중 / ✅ 완료) 열로 전체 항목을 한 화면에 훑어볼 수 있게 한다.
  3. **단계별 상세** — 각 항목마다 체크박스(`- [ ]`/`- [x]`)와 함께 배경·이유, 구현 메모, 복잡도를 적는다.
  4. **다음에 결정할 것** — 미확정 사항이 있을 때만 채우고, 없으면 "현재 없음"으로 비워둔다.
- 항목을 추가·재배치할 때는 요약표와 상세 설명, 단계 기준 표의 진행률을 함께 갱신해 서로 어긋나지 않게 한다.

### 완료 판정 기준 (루프 종료 조건)
- 코딩 결과를 점검해 🔴(치명적)·🟡(중요) 결함이 0개이면 "완성"으로 판정한다.
- 남아 있는 🟢(선택적 개선)는 강제 반영하지 않고, 목록으로 투명하게 보고만 한 뒤 멈춘다.
- 즉, 코드를 다시 점검했을 때 새로운 🔴·🟡 결함이 나오지 않는 상태가 목표다.
- (현실적으로 🟢 개선거리는 항상 존재할 수 있으므로, 이를 0으로 만드는 것은 완료 조건이 아니다.)

### 무한루프 방지 및 중단 보고 규칙
아래 조건에 해당하면 즉시 작업을 중단하고 사용자에게 보고한다.

**중단 조건**
- 반복 횟수 초과: 동일 문제 해결 시도가 10회를 넘어도 완료 판정에 도달하지 못한 경우
- 답보 상태: 수정해도 같은(또는 유사한) 오류가 다시 발생해 개선이 정체된 경우
- 순환 모순: A를 고치면 B가 깨지고, B를 고치면 A가 깨지는 식으로 맴도는 경우
- 컨텍스트 한계: 컨텍스트 창이 80% 이상 소진되어 정확한 판단이 어려운 경우

**중단 시 보고 내용**
- 현재까지 몇 회 반복했는지
- 어떤 문제가 반복되고 있는지 (반복 원인)
- Claude가 보는 막힌 이유와 가능한 선택지 (기준 완화 / 보류 / 방향 전환 / 새 세션 시작)
- 사용자의 결정을 받은 뒤에만 재개한다.

### 적용 범위 및 경계선
- 자율 완결은 "이미 승인된 설계·기능 범위 안에서의 구현·테스트·최적화"에 한정한다.
- 아래 항목은 반드시 사용자 승인 후 진행한다.
  - 설계·기능 구조의 변경
  - 의존성 패키지 추가·제거
  - 데이터 삭제·파일 권한 변경 등 되돌리기 어려운 작업

### Git 커밋 기준
- 🔴·🟡 결함이 0개인 상태에서만 커밋한다.
- 커밋 메시지는 "무엇을 왜 변경했는지"를 명시한다.
- 대규모 리팩토링 전에는 별도 브랜치를 생성한다.

### 토큰 효율 규칙
- 파일 전체를 불필요하게 반복 출력하지 않는다.
- 변경이 필요한 부분만 정확히 수정한다.
- 긴 파일은 필요한 섹션만 읽고 작업한다.

### 앱 완성 후 사용자 매뉴얼 제작 규칙
- 앱 코딩·검증이 모두 완료되어 "완성" 판정이 나면, 사용자 매뉴얼 제작을 권장한다.
  → "앱이 완성되었습니다. 처음 사용하는 분을 위한 사용자 매뉴얼(PDF)을 제작할까요?"
- 사용자가 승인한 경우에만 매뉴얼 제작을 시작한다.

## 개발 중 개선안 제안 규칙
- 각 개발 단계 진행 중 더 나은 방법·구조·기능이 발견되면 즉시 사용자에게 제안할 것
- 제안 시 현재 방식과 개선안을 비교해서 설명하고, 사용자가 채택 여부를 결정
- 설계·기능 변경은 사용자 승인 없이 임의로 변경하지 않음
- 제안은 간결하게, 쉬운 말로 설명할 것

## 파일 작업 절차 원칙 (신설, 2026-07-21 — 앱개발지침 §5 반영)
- 파일 소스 우선순위: ①Filesystem MCP 직접 읽기 ②이번 요청에 새로 첨부된 파일 ③대화 context 내 이전 파일(최후 수단, "이전 버전일 수 있음" 명시)
- "검토해" 요청 시 context에 내용이 있어도 위 우선순위에 따라 재확인 후 시작한다.
- "구버전/불일치/누락" 등을 발견했다고 판단되면 즉시 단정하지 말고 수정일자·로컬 파일을 확인 후 보고한다.

## UI·앱 정보 표시 원칙 (신설, 2026-07-21 — 앱개발지침 §1·§7 반영)
- UI 초기 언어는 한국어를 기본으로 한다.
- 개발자명과 최초 개발일·최근 수정일을 기록한다. 표시 위치·날짜 기준은 사정에 따라 결정하되, git 커밋일자 등 자동 산출 방식을 우선 검토한다.
- 다국어(i18n) 전환은 해외 출시 준비 단계(유료화 로드맵 초입 또는 직전)에 별도 세부 단계로 배치한다. 구조만 미리 열어두는 준비(텍스트를 함수 호출로 분리 등)는 그 이전부터 시작해도 된다.
  - 🟡 후속조치: ROADMAP-CHECKLIST 5단계 "다국어(i18n) 지원 준비" 항목으로 실행 계획 반영함(2026-07-21).

## 로컬 데이터 저장 원칙 (신설, 2026-07-21 — 앱개발지침 §10 반영)
- 저장 데이터에 스키마 버전을 기록하고, 구조 변경 시 구버전 데이터를 자동 변환(마이그레이션)하는 규칙을 적용한다.
- 로컬 데이터 암호화 보관을 기본으로 검토한다.
- 데이터 유실 방지를 위한 주기적 자동 로컬 백업을 검토한다.
- "완전 백업/복원"(자체 형식)과 "공유·타 앱 호환용 내보내기"(표준 형식)는 구분해서 제공하는 것을 검토한다.
- 🟡 후속조치: 현재 storage.js 데이터 모델에 스키마 버전 필드가 없음 — v2 저장소 구조에 추가 필요(ROADMAP-CHECKLIST 1단계 참고).

## 유료화 대비 개발 원칙 (신설, 2026-07-21 — 앱개발지침 §7 반영, v2 유료화 확정에 따라 적용)
- 유료 검증은 최초 실행 시 1회 및 주기적 1회만 온라인 확인 → 평상시 오프라인 사용 보장.
- 유료화 메뉴는 초기 화면에 노출하지 않는다.
- 계정 구조·권한 레이어·결제 연동 포인트는 처음부터 설계에 반영한다.
- 유료화·클라우드 동기화 단계에서 개인정보처리방침, 결제 관련 법적 신고 요건을 검토한다.
- 개발자가 승인한 특정 사용자에게는 결제 없이 무료 제공하는 경로(재사용 가능한 라이선스 키, 키+이메일 조합 검증)를 함께 검토한다.
  - 🟡 후속조치: ROADMAP-CHECKLIST 3단계 "승인 기반 무료 라이선스 구현" 항목으로 실행 계획 반영함(2026-07-21).
- **구매 복원(재설치 시 재결제 방지)**: 결제 상태는 로컬 저장소가 아니라 이메일(계정) 등 서버 측 식별자에 연결한다. 앱을 삭제 후 재설치해도 같은 이메일로 재인증하면 결제 대행 서비스가 기존 구매 이력을 확인해 유료 기능을 자동 복원한다(재결제 요구 금지) — 위의 최초1회+주기적 온라인 확인 시점에 포함시킨다.
- **멀티 플랫폼 주의 (신설, 2026-07-21)**: 결제 확인·구매 복원은 "동기화(멀티기기) 대비 개발 원칙"의 콘텐츠 동기화와는 별개 기능이다. 튜브폴더는 크롬 확장(데스크톱)과 PWA(휴대폰 웹, v1 매니저 페이지)로 나뉘어 배포되는데, 콘텐츠 동기화가 돼도 결제 확인은 자동으로 따라오지 않는다 — PWA에도 결제 확인을 별도로 붙여야 한다.
  - 🟡 후속조치: 현재 3단계 "결제 연동 구현"은 크롬 확장 기준으로만 계획돼 있고, PWA용 결제 확인은 체크리스트에 별도 항목으로 없었음 — ROADMAP-CHECKLIST 3단계에 신규 항목으로 추가함.
- **유료→무료 전환 대비 원칙 (신설, 2026-07-27 — 산들 요청: "유료 판매 목적으로 개발했지만 무료 배포로 결정하면 언제든 전환할 수 있게")**: 유료화 앱이라도 개발자가 이후 "이 앱은 무료로 풀겠다"고 결정할 가능성을 항상 열어둔다. 결제 상태를 참조하는 모든 로직(폴더·영상 개수 제한, 동기화 등 부가기능 잠금)이 단일 진입점(`getCachedLicense()`류 함수) 하나만 거치도록 설계해, 전환 시 그 진입점 한 곳만 고치면 되도록 한다 — 조건문을 코드 곳곳에서 하나씩 찾아 지우는 방식은 지양.
  - ✅ 구현 완료(2026-07-27, tubefolder-extension-v2): `src/license/licenseEngine.ts`의 `FREE_DISTRIBUTION_MODE` 상수(기본값 `false`) — `true`로 바꾸고 재빌드·재배포하면 전체 사용자가 결제 여부와 무관하게 모든 기능을 쓸 수 있게 됨. 빌드 시점 상수라 이미 설치된 사용자에게는 새 버전 배포 후에만 반영된다 — 즉시 반영되는 실시간 전환이 필요하면 원격 설정 등 별도 설계·네트워크 호출·보안 검토가 필요하므로 별도 승인 후 진행(ROADMAP-CHECKLIST 3단계 "유료→무료 전환 스위치 구현" 참고).

## 동기화(멀티기기) 대비 개발 원칙 (신설, 2026-07-21 — 앱개발지침 §8 반영, v2 모바일 동기화 적용)
- 동기화 저장 위치는 "앱 전용 숨김 영역"(예: 구글드라이브 App Data 폴더)을 기본값으로 하고, 육안 확인을 원하면 보이는 폴더도 선택지로 제공한다.
- 자동 동기화 실패는 조용히 재시도, 사용자가 직접 누른 수동 동기화 실패는 즉시 알린다.
- 수동 동기화 버튼은 상시 노출 + 상태(진행중/최신/실패) 시각 표시를 기본 요건으로 한다.
- 오프라인 우선: 시작 시 블로킹 로딩 없이 로컬 데이터로 즉시 실행, 원격 확인·병합은 백그라운드 처리.
- 버전 보관 기준: 개별 항목 단위 이력은 횟수 기준, 일괄 작업은 날짜 기준을 적용한다.
- 모든 데이터 항목에 수정시각·기기ID·버전번호 메타데이터 필드를 개발 초기부터 포함해 향후 정교한 자동 병합 방식으로 업그레이드할 여지를 남긴다.
- 🟡 후속조치: 이미 완료된 "1단계 저장소 백엔드 결정"에 이 메타데이터 필드가 반영되지 않음 — 3단계 동기화 착수 전 재검토 필요(ROADMAP-CHECKLIST 1단계 참고).

## 외부 서비스 연동 UX 원칙 (신설, 2026-07-21 — 앱개발지침 §5 반영, 결제·클라우드 동기화 등에 적용)
- 기능을 켜지 않을 경우 발생하는 결과를 사용자가 선택하기 전에 안내한다.
- 기능 활성화 직후 상태 변화를 확인 팝업 등으로 명확히 고지한다.
- 외부 서비스의 무료 한도·가격·정책을 안내할 때는 정책 변경에 따라 달라질 수 있음을 함께 명시한다.
- 외부 서비스로 넘어가 사용자 조작을 기다려야 하는 구간은 시각적으로 강조된 박스로 표시한다.
- 상태 변화 시점마다 아이콘과 함께 짧은 텍스트 메시지를 병행 표시한다.
- 완료 안내 팝업은 배경 화면에 가려지지 않도록 표시 우선순위를 분리한다.
- 완료 안내 팝업 확인 시 그 팝업을 띄운 하위 화면까지 함께 닫는다.

## 삭제·보관정책(휴지통) UX 원칙 (신설, 2026-07-21 — 앱개발지침 §5 반영)
- 보관기간·자동 영구삭제 시점을 휴지통 목록 화면에 상시 배너로 안내한다.
- 보관기간은 사용자가 조정 가능하도록 검토하고, "자동 삭제 없음" 옵션도 선택지에 포함하는 것을 검토한다.
- 보관기간 변경은 기존 보관 중인 항목에도 소급 적용하는 것을 기본값으로 한다.
- 보관기간을 줄여 기존 항목이 즉시 영구 삭제될 수 있는 경우, 적용 전 영향받는 항목 수를 알리고 확인을 받는다.
- 신규 사용자가 정책을 인지 못한 채 반복 삭제하지 않도록, 초기 사용 기간 동안 삭제 시점에도 관리 화면과 동일한 안내를 노출한다.
  - ✅ 구현 완료(2026-07-27, tubefolder-extension-v2): 삭제(휴지통 이동) 시 보관기간 정책 안내 팝업 + "다음부터 이 안내를 보지 않기" 체크로 반영함. 아래 "주의사항 안내 UX 원칙"으로 일반화함.

## 주의사항 안내 UX 원칙 (일반, 신설 2026-07-27 — 위 휴지통 정책 안내 팝업 사례를 일반 규칙으로 확장, 같은 날 "경고" 등급 추가)
- **적용 대상**: 되돌리기 어렵거나(영구 삭제 등) 사용자가 기본 동작을 놓치기 쉬운 정책이 걸린 기능. 판단 기준은 "앱 완성 후 사용자 매뉴얼 제작 규칙"의 매뉴얼 "주의사항" 섹션에 들어갈 만한 내용인지로 가늠한다 — 매뉴얼에 넣을 만하면 앱 안에서도 이 방식으로 안내한다.
- 해당 기능을 실제로 사용하는 시점(버튼 클릭 등 동작 직전)에 설명 팝업을 먼저 띄운다. 문서에만 적어두고 끝내지 않는다.
- **등급을 둘로 나눈다** — 착수 시 대상 기능이 둘 중 어디에 해당하는지 먼저 판단한다:
  - **주의사항(기본)**: 팝업에 "다음부터 이 안내를 보지 않기" 체크박스를 포함한다. 체크한 채로 진행하면 그 선택을 영구 저장해 이후에는 더 가벼운 한 줄 확인만 노출하고, 체크하지 않으면 다음에도 동일하게 다시 안내한다. 되돌릴 여지가 있는 동작(예: 휴지통으로 이동 — 보관기간 안에는 복구 가능)이 대상.
  - **경고 사항**: "다음부터 보지 않기" 선택지 자체를 주지 않는다 — 사용자의 선택과 무관하게 해당 기능을 실행할 때마다 매번 팝업한다. 즉시·영구적으로 되돌릴 수 없는 동작(예: 휴지통 비우기=영구 삭제)이 대상.
- 최초 구현 사례(참고용 기준 구현): 주의사항 등급은 휴지통 보관기간 안내 팝업, 경고 등급은 휴지통 비우기(영구 삭제) 확인(둘 다 tubefolder-extension-v2) — 후자는 애초에 다시 보지 않기 옵션 없이 매번 확인받도록 만들어져 있었고, 이번에 이 규칙의 "경고" 등급 기준 구현 사례로 지정함(2026-07-27, 코드 변경 없음).
- 구현 메모: "주의사항" 등급의 다시 보지 않기 플래그는 `Settings`에 `boolean`으로 저장한다. 안내 대상 기능이 여러 개로 늘어나면 플래그를 하나씩 늘어놓기보다 "기능 키 → 다시 보지 않음 여부" 형태의 공용 구조로 통합하는 것을 검토한다(기존 저장 구조 변경에 해당하므로 착수 전 사용자 승인 필요). "경고" 등급은 저장할 상태 자체가 없다(매번 새로 판단).

## Claude Code 작업 효율화 팁 (신설, 2026-07-21 — 앱개발지침 §5 반영)
- 프로젝트별 `.claude/settings.local.json`에 안전한 반복 명령(빌드·테스트·npm/git 조회·커밋 등)은 미리 허용 규칙으로 등록해 승인 프롬프트 피로도를 줄인다.
- 단, 파일 삭제·강제 push 같은 파괴적·비가역적 명령은 자동 허용에서 제외하고 가능하면 `permissions.deny`로 명시 차단한다.
