# 튜브폴더(TubeFolder) 개발 인계서

작성일: 2026-07-25 (저녁) · 새 대화방에서 이어가기 위한 인계 문서입니다.
(CLAUDE.md는 새 대화에도 자동 로드되므로, 여기서는 CLAUDE.md·ROADMAP-CHECKLIST.md에 없는 "이번 세션에 실제로 있었던 일과 후속조치 필요 항목" 위주로 정리했습니다. 이전 인계서(같은 파일명, 2026-07-25 오후자)는 이 내용으로 완전히 대체됩니다.)

---

## 0. 인계서가 꼭 필요한가?

원칙적으로는 아닙니다. CLAUDE.md·ROADMAP-CHECKLIST.md가 매 작업마다 실시간으로 갱신되므로 새 대화방에서 이 두 파일만 읽어도 "지금 뭐가 어디까지 됐는지"는 파악됩니다. 이 인계서는 체크리스트에 다 담기지 않는 것들(이번 세션의 판단 근거·발견한 버그·환경 특이사항) 위주로만 정리합니다.

---

## 1. 프로젝트 현재 상태 요약

| 구분 | tubefolder-extension-v1/ | tubefolder-extension-v2/ |
|---|---|---|
| 상태 | **완성·배포됨** (건드리지 않음 원칙, 이번 세션 중 산들 요청으로 README.md 오타만 예외 수정) | **개발 진행 중** — 1단계 5/5, 2단계 3/3, 3단계 2/5 완료 |
| 기술 | 순수 JS/HTML/CSS (빌드 없음) | React + TypeScript + Vite |
| 배포 URL | `https://fics01287-arch.github.io/tube-folder/tubefolder-extension-v1/manager.html` | `https://fics01287-arch.github.io/tube-folder/tubefolder-extension-v2/pwa-dist/` (PWA, 실기기 동기화 검증 완료) |

- 자세한 진행 상황·결정 근거는 전부 **`tubefolder-extension-v2/ROADMAP-CHECKLIST.md`**에 있습니다. 새 세션에서 v2 작업을 이어가면 이 파일부터 읽고 시작하세요.
- Git은 **현재 완전히 푸시된 상태**입니다(마지막 확인 시 unpushed 커밋 0개). 새 세션에서 굳이 먼저 push할 필요 없습니다.

---

## 2. 이번 세션에서 완료한 작업

1. **v2를 독립 PWA로 신규 빌드 + 구글 드라이브 동기화 연결** (3단계 "PWA에 동기화 붙이기" 완료)
   - 착수 전 중요한 판단: 체크리스트 문구("PWA 매니저에 동기화 붙이기")가 가리키는 "PWA"가 뭔지 불명확했음 — 실제로 존재하는 유일한 PWA는 v1의 `manager.html`뿐이었고, v2는 이때까지 크롬 확장으로만 빌드됐음. v1은 "건드리지 않음" 원칙과 충돌하고 데이터 스키마도 달라서(v1엔 schemaVersion·deviceId·version 필드 없음), **산들 승인으로 "v1은 절대 안 건드리고, v2를 새 독립 PWA로 빌드"하는 쪽으로 확정**.
   - 구현: `index.pwa.html`·`vite.pwa.config.ts`·`manifest.webmanifest`·`sw-pwa.js` 신설(크롬 확장 빌드와 완전히 분리된 산출물 `pwa-dist/`). 인증은 `chrome.identity`가 없는 환경이라 Google Identity Services(GIS) 웹 로그인 방식을 새로 구현 — 기존 Drive REST 로직(`googleDrive.ts`)을 `driveBackendBase.ts`로 공통 추출해 확장(chrome.identity)·PWA(GIS) 양쪽이 재사용.
   - 산들이 GCP 콘솔에서 **"웹 애플리케이션" 유형 OAuth 클라이언트를 새로 발급**(확장용과는 별개 클라이언트)하고 코드에 반영, 실기기(네이버 브라우저)에서 로그인·폴더 생성·"방금 전 동기화됨" 배지까지 실제 클릭으로 검증 완료.
   - **실기기 검증 중 발견·수정한 버그 2건**:
     - ① 카카오톡 인앱 브라우저로 링크를 열면 "구글 로그인 스크립트를 불러올 수 없음" 오류 — 인앱 브라우저가 구글 로그인을 차단하는 것으로 보임(일반적으로 알려진 제약). **코드 문제 아님** — 일반 브라우저(Chrome/네이버 등)로 열어야 함. 5단계 사용자 매뉴얼에 안내 필요(체크리스트에 기록해둠).
     - ② 일반 브라우저에서도 "연결" 버튼을 누르면 로그인 창 대신 구글 고객센터 안내 문서로 튀는 문제 발생 — GIS 스크립트를 클릭 시점에야 로드하기 시작해 생기는 시간차 때문에 모바일 브라우저가 로그인 팝업을 "사용자가 직접 연 것"으로 인정하지 않고 막은 것으로 추정. **`preloadGis()`(페이지 로드 시 미리 스크립트 준비) 추가로 수정**, 재검증 통과.
   - 커밋: `d313d5f`(PWA 빌드+GIS 구현) → `5c8f758`(클라이언트 ID 반영) → `7cf7eac`(팝업 차단 버그 수정) → `6dcc849`(검증 완료 기록).

2. **결제 연동(ExtensionPay) 구현 — 코드는 완료, 산들의 가입만 남음**
   - 서비스 조사: Chrome 자체 인앱결제는 2021년 완전 종료 재확인. ExtensionPay·Fungies.io·Dodo Payments 비교 후 **ExtensionPay 채택**(확장 전용 설계, 서버 불필요, 이메일 기반 "구매 복원" 기본 내장이 결정 근거).
   - **중요한 설계 문제와 해결**: `extpay` 패키지는 내부적으로 `webextension-polyfill`을 쓰는데, 이게 브라우저 확장이 아닌 환경(PWA)에서 로드되면 즉시 에러를 던짐. 매니저 UI(App.tsx 등)는 확장·PWA 공용 번들이라 잘못 import하면 **PWA 빌드 전체가 깨지는 위험**이 있었음 → `licenseEngine.ts`(순수 캐시, extpay 미import, 항상 안전) / `licenseManager.ts`(extpay를 동적 import로만 사용, 확장 컨텍스트일 때만 실제 실행) / `background.ts`(정적 import, 확장 전용 번들이라 안전) 3단으로 분리해서 해결. 빌드 산출물에서 PWA는 이 코드가 담긴 청크를 아예 안 받아간다는 것까지 확인함.
   - 무료/유료 정책(폴더 20개·영상 500개 한도, 재생목록 대량가져오기 캡, 동기화는 PRO 전용) 구현. **단, 이 한도는 "확장 컨텍스트 + 결제 설정 완료" 상태일 때만 걸림** — PWA는 아직 유료 확인 수단이 없어서(별도 항목), 지금 걸면 PWA 사용자가 영영 못 벗어나는 상태가 되므로 의도적으로 열어둠.
   - `EXTPAY_EXTENSION_ID`는 아직 플레이스홀더 — **산들이 extensionpay.com 가입 + Stripe 연결 + 가격 설정(1회 결제 ₩15,000~20,000) 후 ID를 알려주면 반영·재빌드**할 예정. 가입 전까지는 결제 관련 기능이 전부 비활성(개발 방해 없음).
   - 커밋: `1405c8f`.

3. **v1 README.md 오타 수정 (산들 요청, "건드리지 않음" 원칙의 명시적 예외)**
   - 실제 폴더명은 `tubefolder-extension-v1`인데 README 3곳이 구버전 이름(`tubefolder-extension`)을 참조하고 있던 걸 산들이 직접 발견 → 문서 전용 수정 요청받아 진행(manifest.json 등 실제 확장 파일은 무관, 동작 영향 없음). 커밋 `5d9234a`.

---

## 3. Git / 환경 상태 (직접 확인, 2026-07-25 저녁 기준)

- **푸시 상태: 완전히 최신**(unpushed 커밋 0개 확인). 이번 세션은 작업 직후 바로바로 커밋+푸시하는 방식으로 진행함.
- **샌드박스 환경 제약(계속 재현됨)**: `.git/index.lock`·`.git/HEAD.lock` 등 잔여 lock 파일이 거의 매 git 명령마다 발견됨. 삭제(unlink)가 막혀 있으면 `mv 파일명 파일명.bak_숫자`로 치우고 재시도하면 됨 — 이번 세션에도 수십 번 반복됨, 새 세션에서도 동일할 것으로 예상.
- **npm/node_modules 플랫폼 불일치**: 이 리눅스 샌드박스에 마운트된 `node_modules`는 산들의 Windows 환경 기준이라 rollup 네이티브 바이너리가 안 맞아 실 폴더에서 바로 빌드가 안 됨. 매번 `/tmp`에 소스만 복사(`rsync --exclude node_modules --exclude dist --exclude pwa-dist`) 후 그 자리에서 `npm install`(리눅스용 바이너리 새로 받음) → 빌드 → 결과물(`pwa-dist/` 등)만 실 폴더로 복사하는 방식으로 우회함. `package-lock.json`은 새 의존성(`extpay`) 추가 시 이 방식으로 갱신 후 실 폴더에 반영해둠.
- **`tubefolder-extension-v1.zip`가 저장소 루트에 생김(untracked, 1.87MB)** — 산들이 v1 배포 테스트로 직접 압축하신 파일로 보임. git에 커밋하지 않고 그대로 뒀습니다(배포 산출물이라 소스 관리 대상 아님). 필요 없으면 그냥 지우셔도 되고, 앞으로 이런 zip이 실수로 커밋되지 않게 `.gitignore`에 패턴을 추가하는 것도 검토해볼 만합니다(원하시면 다음에 처리하겠습니다).

---

## 4. 새 대화에서 참고할 것

- v2 3단계 남은 3개: **결제 연동(ExtensionPay 가입 대기 중)** / PWA 결제 확인 붙이기 / 승인 기반 무료 라이선스 — `ROADMAP-CHECKLIST.md` "한눈에 보기" 표 참고.
- **산들이 할 일이 남아있는 것**: extensionpay.com 가입 → Stripe 연결 → 확장 등록 → 가격 플랜(1회 결제 ₩15,000~20,000) 설정 → extension id를 알려주면 코드에 반영. (`tubefolder-extension-v2/README.md`의 "유료화(ExtensionPay) 사용 준비" 절 참고)
- 인앱 브라우저(카카오톡 등)에서 구글 로그인이 막히는 현상 확인됨 — 5단계 "사용자 매뉴얼 작성" 때 "일반 브라우저에서 열어주세요" 안내 문구 넣을 것.
- 새 기능 빌드 검증할 때는 위 "npm/node_modules 플랫폼 불일치" 우회 방법을 그대로 쓰면 됨(`/tmp`에 복사 후 설치·빌드).
- 커밋 직전 새로 생긴 파일 중 민감한 것(키·토큰 등)이 gitignore에 빠져있지 않은지 확인하는 습관 계속 유지.

---

## 5. 참고 파일 위치

- 프로젝트 지침: `tube-folder/CLAUDE.md`
- v2 개발 체크리스트: `tube-folder/tubefolder-extension-v2/ROADMAP-CHECKLIST.md`
- v2 README(빌드·PWA·동기화·유료화 사용 준비 방법): `tube-folder/tubefolder-extension-v2/README.md`
- v1 사용자 매뉴얼: `tube-folder/tubefolder-extension-v1/docs/튜브폴더_사용자매뉴얼.pdf`
- GitHub 저장소: https://github.com/fics01287-arch/tube-folder
- 배포된 v1 웹앱(PWA): https://fics01287-arch.github.io/tube-folder/tubefolder-extension-v1/manager.html
- 배포된 v2 웹앱(PWA, 신규): https://fics01287-arch.github.io/tube-folder/tubefolder-extension-v2/pwa-dist/
