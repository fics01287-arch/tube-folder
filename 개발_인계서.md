# 튜브폴더(TubeFolder) 개발 인계서

작성일: 2026-07-25 · 새 대화방에서 이어가기 위한 인계 문서입니다.
(CLAUDE.md는 새 대화에도 자동 로드되므로, 여기서는 CLAUDE.md·ROADMAP-CHECKLIST.md에 없는 "이번 세션에 실제로 있었던 일과 후속조치 필요 항목" 위주로 정리했습니다. 이전 인계서(같은 파일명, 2026-07-21자)는 이 내용으로 완전히 대체됩니다.)

---

## 0. 인계서가 꼭 필요한가? (산들 질문에 대한 답)

원칙적으로는 아닙니다. CLAUDE.md·ROADMAP-CHECKLIST.md가 매 작업마다 실시간으로 갱신되고 있어서, 새 대화방에서 이 두 파일만 읽어도 "지금 뭐가 어디까지 됐는지"는 전부 파악됩니다. 체크리스트 자체는 그래서 별도 인계서가 없어도 됩니다.

인계서가 따로 의미 있는 이유는 체크리스트에 안 남는 것들 때문입니다 — 이번 세션에서 발견한 보안 이슈, git 미푸시 상태, 샌드박스 환경 특이사항, 대화 중에만 오간 판단 근거 같은 것들입니다. 아래 내용이 그것입니다.

---

## 1. 프로젝트 현재 상태 요약

| 구분 | tubefolder-extension-v1/ | tubefolder-extension-v2/ |
|---|---|---|
| 상태 | **완성·배포됨** (건드리지 않음) | **개발 진행 중** — 1단계 5/5, 2단계 3/3, 3단계 1/5 완료 |
| 기술 | 순수 JS/HTML/CSS (빌드 없음) | React + TypeScript + Vite |
| 배포 URL | `https://fics01287-arch.github.io/tube-folder/tubefolder-extension-v1/manager.html` | 없음(아직 미배포) |

- 자세한 진행 상황·결정 근거는 전부 **`tubefolder-extension-v2/ROADMAP-CHECKLIST.md`**에 있습니다. 새 세션에서 v2 작업을 이어가면 이 파일부터 읽고 시작하세요.

---

## 2. 이번 세션에서 완료한 작업

1. **지침 3파일 정합성 점검·반영** (Chat + `공통지침/앱개발지침 작성용 지침.txt`, tube-folder 밖에 있는 별도 폴더)
   - `앱개발지침 작성용 지침.txt`와 `CLAUDE.md`·`ROADMAP-CHECKLIST.md`를 비교해 누락된 원칙 다수 발견 → CLAUDE.md에 8개 신규 섹션(파일작업절차·UI앱정보·로컬데이터저장·유료화대비·동기화대비·외부서비스UX·휴지통UX·Claude Code팁) 반영.
   - 대화 중 추가로 발견된 3가지를 세 파일(CLAUDE.md / ROADMAP-CHECKLIST.md / 공통지침의 앱개발지침 작성용 지침.txt)에 함께 반영: ①구매 복원(재설치 시 재결제 방지) 원칙 ②멀티 플랫폼 배포 시 결제 확인은 콘텐츠 동기화와 별개라는 원칙(PWA에도 결제 확인 별도 필요) ③다국어·승인 기반 무료 라이선스를 원칙 문장만 두지 않고 체크리스트 실행 항목으로 등록.
   - **`앱개발지침 작성용 지침.txt`는 git 추적 대상이 아닙니다** (별도 폴더, 공통지침). 커밋 이력에 안 남으니, 다음 세션에서 "지침 검토해줘" 하면 이 파일도 직접 다시 읽어서 확인해야 최신 상태를 압니다.
   - CLAUDE.md 변경분은 한동안 커밋 안 된 채 쌓여있다가(작업 도중 Chat에서 직접 파일 수정 → git 커밋은 별도 단계라 누락됨), 나중에 발견해서 커밋 `95d2ee7`로 정리했습니다.

2. **v2 1단계 완전 종료 (5/5)**: "스키마 버전 + 동기화 메타데이터 필드 추가" — `DATA_VERSION` 1→2, 노드에 `schemaVersion`·`deviceId`·`version` 필드 추가 + 구버전 데이터 자동 마이그레이션. tsc·3빌드 통과, 실클릭 검증. 커밋 `653c7c7`.

3. **v2 2단계 완전 종료 (3/3)**: "이어보기(재생 위치 기억)" 구현 — v1의 `window.open` 방식 대신 유튜브 IFrame Player를 오버레이 팝업으로 내장. 코딩 전 Claude Code가 UX 질문 4가지(재생창 위치·자동이어재생 여부·저장 주기·완료 시 리셋)를 먼저 묻고 산들 승인 받은 뒤 착수. MV3 CSP 때문에 유튜브 공식 스크립트 로더를 못 써서 postMessage 프로토콜 직접 구현으로 우회. 개발 중 레이스 컨디션 버그(오버레이 닫을 때 저장이 안 끝난 상태로 화면 전환돼 최대 7초 전 위치에서 재생되는 문제) 발견 즉시 수정. 커밋 `b8b76c3`.

4. **v2 3단계 착수 — "모바일 자동 동기화(구글 드라이브)" 완료**: 원드라이브와 비교 조사한 결과 구글 드라이브가 명확히 우위(숨김 전용 폴더, 최소 권한 스코프, 크롬 네이티브 OAuth 지원)라 구글 드라이브부터 구현, 원드라이브는 어댑터 인터페이스만 열어두고 보류. LWW(마지막 저장 우선) 병합 + 삭제 전파용 tombstone(삭제 기록) 저장구조 추가(승인받음) + 동기화 트리거 4종(시작 시/변경 후 디바운스/15분 주기 알람/수동 버튼). 산들이 직접 Google Cloud Console에서 프로젝트 생성 → 2단계 인증(MFA) 활성화 → OAuth 클라이언트 ID 발급까지 진행하고, 실제 크롬 확장에서 OAuth 연결·드라이브 동기화까지 종단 검증 완료. 커밋 `8b82ee6`(구현) → `4e254f8`(클라이언트 ID 반영+완료). "PWA에 동기화 붙이기"는 별도 후속 항목으로 체크리스트 3단계에 분리 등록(아직 미착수).

5. **🔴 보안 이슈 발견·즉시 수정**: 동기화 기능 커밋 직전, `dev-key.pem`(OAuth 클라이언트 ID 고정용 확장 개인 키)이 `.gitignore`에 등록 안 된 걸 Chat에서 git 상태 직접 확인으로 발견했습니다. 그대로 커밋했다면 개인 키가 공개 GitHub 저장소에 노출될 뻔했습니다. Claude Code에 즉시 수정 지시 → `.gitignore`에 추가, 스테이징된 이력이 없어 오염 없이 안전하게 넘어감. **앞으로도 커밋 직전엔 새로 생긴 민감 파일이 gitignore에 빠져있지 않은지 한 번씩 확인하는 습관이 필요합니다.**

6. **체크리스트 표시 형식 지정**: 산들이 "체크리스트 보여줘" 요청 시 4단 전체 구성(① 단계 기준 → ② 한눈에 보기 → ③ 단계별 상세 → ④ 다음에 결정할 것)으로 보여달라고 요청 — 앞으로 이 형식으로 응답합니다. (이건 Chat 세션 내 지침이라 자동으로 다음 대화에 넘어가지 않을 수 있으니, 새 대화에서 다르게 나오면 다시 알려주세요.)

---

## 3. Git / GitHub 현재 상태 (확인된 사실, 2026-07-25 기준 직접 확인)

- 원격 저장소: `https://github.com/fics01287-arch/tube-folder.git` (Public)
- **로컬 master가 origin/master보다 9커밋 앞서 있습니다 (미푸시 상태)** — `653c7c7`부터 `010b0e4`까지 이번 세션 작업 전부가 아직 GitHub에 올라가지 않았습니다. 다음 세션에서 "push해줘"라고 하면 됩니다(푸시 인증은 이미 설정돼 있음).
- **샌드박스 환경 제약(계속 재현됨)**: 이전 세션과 동일하게 `.git/index.lock` 등 잔여 lock 파일이 종종 남습니다. 삭제(unlink)가 막혀 있으면 `mv 파일명 파일명.bak_숫자`로 치우고 재시도하면 해결됩니다.
- Google Cloud 프로젝트 이름에 오타 있음(`tubefolde`, r 누락) — 프로젝트 ID라 생성 후 수정 불가하지만, 개발자만 보는 내부 식별자라 기능엔 전혀 영향 없습니다. 신경 안 쓰셔도 됩니다.

---

## 4. 새 대화에서 참고할 것

- 새 세션 시작하면 **먼저 `git push`부터 처리**하는 걸 권장합니다(위 3번 참고).
- v2 다음 작업: 3단계 남은 4개(PWA 동기화 붙이기 / 결제 연동 구현 / PWA 결제 확인 / 승인 기반 무료 라이선스) 중 택1 — `ROADMAP-CHECKLIST.md` "한눈에 보기" 표 참고.
- 🟡 배포(프로덕션 게시) 시점에 Google Cloud 콘솔 "데이터 액세스" 범위를 정식 등록해야 함 — ROADMAP-CHECKLIST.md 해당 항목에 기록해둠, 지금 당장은 아님.
- Claude Code에서 작업 시 프로젝트 폴더를 tube-folder로 명시 지정할 것(다른 프로젝트와 헷갈리지 않도록).
- 커밋 직전 새로 생긴 파일 중 민감한 것(키·토큰 등)이 gitignore에 빠져있지 않은지 확인하는 습관 유지.

---

## 5. 참고 파일 위치

- 프로젝트 지침: `tube-folder/CLAUDE.md`
- v2 개발 체크리스트: `tube-folder/tubefolder-extension-v2/ROADMAP-CHECKLIST.md`
- v2 README(빌드·확장 로드·동기화 사용 준비 방법): `tube-folder/tubefolder-extension-v2/README.md`
- v1 사용자 매뉴얼: `tube-folder/tubefolder-extension-v1/docs/튜브폴더_사용자매뉴얼.pdf`
- 전체 앱 개발 공통지침(별도 폴더, git 아님): `공통지침/앱개발지침 작성용 지침.txt`
- GitHub 저장소: https://github.com/fics01287-arch/tube-folder
- 배포된 웹앱(PWA): https://fics01287-arch.github.io/tube-folder/tubefolder-extension-v1/manager.html
