# 튜브폴더(TubeFolder) 개발 인계서

작성일: 2026-08-23 · 새 대화방에서 이어가기 위한 인계 문서입니다.
(CLAUDE.md는 새 대화에도 자동 로드되므로, 여기서는 CLAUDE.md·ROADMAP-CHECKLIST.md에 없는 "지금 당장 급한 것"과 "환경 특이사항" 위주로 정리했습니다. 이전 인계서(2026-07-25자)는 이 내용으로 완전히 대체됩니다.)

---

## 0. 인계서가 꼭 필요한가?

원칙적으로는 아닙니다. `ROADMAP-CHECKLIST.md`가 항목마다 날짜·근거까지 실시간으로 갱신되므로 그 파일만 읽어도 "지금 뭐가 어디까지 됐는지"는 파악됩니다. 이 인계서는 그중에서도 **지금 이 순간 가장 급하게 확인해야 할 것**과 체크리스트에 다 담기지 않는 환경 특이사항만 골라서 정리합니다.

---

## 1. 프로젝트 현재 상태 요약

| 구분 | tubefolder-extension-v1/ | tubefolder-extension-v2/ |
|---|---|---|
| 상태 | **완성·배포됨** (건드리지 않음 원칙 유지) | **출시 준비 막바지** — 1~2단계 완료, 3단계 5/6, 4단계 6/7, 5단계 1/5 |
| 결제 | 없음 | Paddle(Merchant of Record), 1회 결제 ₩30,000. 샌드박스 파이프라인 전체 검증 완료, **라이브 전환 진행 중**(아래 2번 참고) |
| Chrome 웹 스토어 | — | 비공개(미등록) 등록 전체 절차 완료, **검토 제출함(배포 보류 설정)** — 결과 대기 중 |

자세한 진행 상황·결정 근거는 전부 **`tubefolder-extension-v2/ROADMAP-CHECKLIST.md`**에 있습니다. 새 세션에서 v2 작업을 이어가면 이 파일부터 읽고 시작하세요.

---

## 2. 지금 당장 확인해야 할 것 (우선순위 순)

1. **Paddle 도메인 재검토 결과** — 8/17에 도메인을 올바른 경로(`fics01287-arch.github.io/tube-folder/tubefolder-extension-v2/pwa-dist/legal`)로 고쳤지만 "다시 제출" 버튼을 안 눌러 6일간 방치돼 있던 것을 8/23에 발견, 재제출 시도했으나 **도메인 편집(연필) 아이콘 자체가 클릭에 반응하지 않는 Paddle 화면 버그**를 발견함(여러 브라우저·키보드 포커스 전부 실패). `sellers@paddle.com`에 수동 처리 요청 메일 발송 대기 중 — **회신 결과부터 확인**할 것.
   - ⚠️ **Paddle 계정 등록 이메일은 `fics01@naver.com`입니다** (`fics01287@gmail.com`이 아님 — 8/23에 제가 잘못 짐작했던 걸 산들이 직접 확인해서 정정함). Paddle 관련 연락은 전부 이 네이버 주소 기준으로 할 것. 다른 서비스(GitHub·Chrome 웹 스토어·GCP)는 기존대로 `fics01287@gmail.com` 유지.
2. **Chrome 웹 스토어 검토 결과** — 8/23 제출 완료(ID `ldhokniendkojmkngaojjlkhememdgog`), "배포 보류" 체크 해제하고 제출해 자동 공개 안 되게 해뒀음. 검토는 몇 주 걸릴 수 있음. **⚠️ 검토 통과 알림을 받으면 그 시점부터 30일 안에 직접 "게시" 버튼을 눌러야 함** — 그 전에 Paddle 라이브 전환(1번 항목)까지 끝내둬야 함. 개발자 대시보드: `https://chrome.google.com/webstore/devconsole/ef8520ed-782d-4e7a-8a43-043fc9529424/ldhokniendkojmkngaojjlkhememdgog/edit`
3. **GCP OAuth Application ID 갱신 (아직 미처리)** — 새 확장 ID(`ldhokniendkojmkngaojjlkhememdgog`)가 발급됐는데, 구글 드라이브 동기화용 OAuth 클라이언트(`864997495294-jugt44j4il53kjcr5m982n0cflfp1252.apps.googleusercontent.com`)는 예전 `key` 필드로 고정돼 있던 ID 기준으로 등록돼 있음 — 이대로면 스토어 배포판에서 구글 드라이브 동기화가 안 될 수 있음. Google Cloud Console에서 이 OAuth 클라이언트의 Application ID를 새 확장 ID로 갱신 필요.
4. Paddle 계정 인증(도메인+본인인증)이 전부 끝나면, ROADMAP-CHECKLIST.md의 "01 실계정을 설정하세요" 절차(카탈로그 재생성 → 결제수단 활성화 → 라이브 체크아웃 링크·웹훅 재발급 → `licenseEngine.ts`/`worker.js`의 샌드박스 값을 라이브 값으로 교체) 진행.

---

## 3. Git / 환경 상태 — 특이사항 (계속 재현됨, 새 세션에서도 동일 예상)

- **마운트 폴더 파일 삭제(unlink)가 항상 막혀 있음.** `rm`은 거의 매번 "Operation not permitted"로 실패함. 해결법: 같은 디렉터리 안에서 `mv 파일명 파일명.bak_시각`으로 이름만 바꾸면 됨(삭제가 아니라 이름변경은 됨). 단, **다른 파일시스템(`/tmp` 등)으로의 `mv`는 안 될 때가 있음** — 그럴 땐 같은 마운트 안에서만 이름을 바꿔서 치워둘 것.
- **git 명령 직전 lock 파일이 자주 남아있음.** `.git/index.lock`·`.git/HEAD.lock`·`.git/refs/remotes/origin/master.lock` 등을 위 방식으로 `mv`해서 치운 뒤 재시도하면 됨. `git commit`/`push` 때마다 `.git/objects/**/tmp_obj_*` unlink 경고가 뜨는데, 이건 무해한 경고이고 커밋 자체는 정상적으로 완료됨(무시해도 됨).
- **디렉터리째로 rename은 잘 됨.** `dist/`·`pwa-dist/`처럼 안에 이전 세션의 orphaned 파일이 쌓여 `npm run build`의 `emptyOutDir`가 실패할 때는, 개별 파일을 치우려 하지 말고 **디렉터리 전체를 `mv dist dist.old-시각`으로 통째로 이름 바꾸면** vite가 새 디렉터리를 깨끗하게 새로 만듦. 이번 세션(8/23 아이콘 교체)에서 이 방법으로 해결함.
- **node_modules가 Windows 기준이라 리눅스 샌드박스에서 rollup 네이티브 바이너리가 없어서 빌드가 처음엔 실패함.** `npm install @rollup/rollup-linux-x64-gnu --no-save --no-package-lock`로 리눅스용 바이너리만 추가 설치하면 그 뒤로는 정상 빌드됨(이번 세션에서 겪고 해결).
- **Chrome 웹 스토어 자체 도메인(`chrome.google.com/webstore/*`)은 Claude in Chrome으로도 스크립팅 불가.** Chrome 플랫폼 자체 제약이라 우회 불가 — 이 작업은 항상 산들이 화면 캡처를 보여주면 그걸 보고 안내하는 방식으로 진행함.

---

## 4. 참고 파일 위치

- 프로젝트 지침: `tube-folder/CLAUDE.md`
- v2 개발 체크리스트(가장 자세한 기록): `tube-folder/tubefolder-extension-v2/ROADMAP-CHECKLIST.md`
- 개인정보처리방침 등 법적 문서(배포된 페이지): `https://fics01287-arch.github.io/tube-folder/tubefolder-extension-v2/pwa-dist/legal`
- GitHub 저장소: https://github.com/fics01287-arch/tube-folder
- Paddle 대시보드: https://vendors.paddle.com/login
- Chrome 웹 스토어 개발자 대시보드(튜브폴더 v2 항목): https://chrome.google.com/webstore/devconsole/ef8520ed-782d-4e7a-8a43-043fc9529424/ldhokniendkojmkngaojjlkhememdgog/edit
