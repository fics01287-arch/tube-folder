node_modules/
dist/
*.local
backup-*.zip
dev-key.pem
# Vite가 빌드(vite build)할 때마다 자동으로 하나씩 남기는 임시 설정 캐시 파일(내용은 매번 같고
# 파일명 타임스탬프만 다름) — 앱 동작과 무관한 빌드 부산물이라 추적 대상에서 제외(2026-09-10,
# git add . 과정에서 실수로 여러 개가 같이 커밋된 것을 발견해 추가).
*.timestamp-*.mjs
