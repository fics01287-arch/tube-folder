# 튜브폴더 버전2 — 개발 체크리스트

작성일: 2026-07-20 (최종 갱신: 2026-07-20)
사용법: "한눈에 보기" 표의 상태 칸을 갱신하며 진행합니다. 상태는 `⬜ 미착수` → `🔧 진행중` → `✅ 완료`로 바꾸고, 완료 시 날짜·커밋을 detail 항목에 적어두면 다음 세션에서 이어가기 쉽습니다. 단계별 진행률(아래 "단계 기준" 표의 진행 상태 칸)도 함께 갱신합니다.

> **양식 안내**: 이 문서의 구성(① 단계 기준 → ② 한눈에 보기 요약표 → ③ 단계별 상세 → ④ 다음에 결정할 것)은 앞으로 이 프로젝트의 체크리스트 기본 틀로 유지합니다. 항목이 추가·완료되어도 이 순서와 형식을 그대로 씁니다.

---

## 단계 기준

| 단계 | 기준 | 진행 상태 |
|---|---|---|
| 1단계 | 구조에 영향 — v2를 짜기 시작하기 *전에* 정해야 나중에 재작업이 없음 | 1/4 완료 |
| 2단계 | 핵심 신규 기능 — 사용자가 가장 체감하는 가치, 구조가 정해진 다음 바로 착수 가능 | 0/3 완료 |
| 3단계 | 고급 기능 — 외부 서비스 연동 등 복잡도·리스크가 높아 핵심 기능이 안정된 뒤 진행 권장 | 0/2 완료 |
| 4단계 | 마무리 다듬기 — 있으면 좋지만 없어도 동작에 지장 없는 항목, 여유 있을 때 처리 | 0/5 완료 |

---

## 한눈에 보기 (진행 현황판)

| 단계 | 항목 | 복잡도 | 상태 |
|---|---|---|---|
| 1. 구조 설계 | 기술 스택 | - | ✅ 결정됨 — React + TypeScript + Vite |
| 1. 구조 설계 | 가상 스크롤 구조 설계 | 중간 | ⬜ 미착수 |
| 1. 구조 설계 | 저장소 백엔드 결정 | 중간 | ⬜ 미착수 |
| 1. 구조 설계 | 유료화 정책 결정 | 중간 | ⬜ 미착수 |
| 2. 핵심 기능 | 우클릭 메뉴 폴더 추가·이름변경·삭제 | 중간 | ⬜ 미착수 |
| 2. 핵심 기능 | 유튜브 재생목록 일괄 가져오기 | 중간 | ⬜ 미착수 |
| 2. 핵심 기능 | 이어보기(재생 위치 기억) | 높음 | ⬜ 미착수 |
| 3. 고급 기능 | 모바일 자동 동기화(구글드라이브/원드라이브) | 높음 | ⬜ 미착수 |
| 3. 고급 기능 | 결제 연동 구현(유료화 실장) | 높음 | ⬜ 미착수 |
| 4. 다듬기 | 폴더 아이콘 다양화 + 초기화 | 낮음~중간 | ⬜ 미착수 |
| 4. 다듬기 | 드래그 삽입선 표시 | 낮음 | ⬜ 미착수 |
| 4. 다듬기 | 영상 duration 정밀 수집 | 낮음 | ⬜ 미착수 |
| 4. 다듬기 | 휴지통 자동 비우기 | 낮음 | ⬜ 미착수 |
| 4. 다듬기 | 접근성 보강 | 낮음 | ⬜ 미착수 |

---

## 1단계 — 구조 설계 (착수 전 결정)

- [x] **기술 스택: React + TypeScript + Vite** — 결정 완료
  - v1(순수 JS)과 달리 빌드 과정이 추가되지만, 이번 v2에서 예정된 기능(가상 스크롤, 아이콘 관리, 결제 연동 등)으로 코드 규모가 커질 걸 감안해 구조적으로 관리하기 쉬운 방식 선택.
  - 참고: 저장소의 `youtube-manager-extension`이 같은 방식으로 시도되다 중단된 뼈대가 있음 — 재사용 가능성 검토.

- [ ] **가상 스크롤 구조 설계**
  - 이유: 폴더 하나에 영상이 많을 때(실측: 8,000개에서 렌더링 중 약 113ms 멈춤) 성능 문제가 실제로 확인됨. 렌더링 방식은 v2 전체 UI 컴포넌트 구조의 기반이라, 나중에 붙이면 관련 코드를 거의 다시 짜야 함.
  - 복잡도: 중간

- [ ] **저장소 백엔드 결정 (localStorage/chrome.storage.local 유지 vs IndexedDB 전환)**
  - 이유: "이어보기" 기능이 영상별 재생 위치 데이터를 추가로 저장해야 하고, 동기화·유료화(라이선스 상태 저장) 기능도 저장 구조에 영향을 주므로 먼저 정해야 함.
  - 복잡도: 중간

- [ ] **유료화 정책 결정**
  - 결정할 것: ① 무엇을 유료로 할지(예: 폴더 개수 제한 해제, 동기화, 재생목록 일괄 가져오기 등 후보 — 실제 항목은 산들이 정함) ② 1회 결제 vs 구독 ③ 가격
  - 이유: 어떤 기능을 유료로 막을지에 따라 2·3단계 기능들의 설계(어디에 "잠금" 로직을 넣을지)가 달라지므로 먼저 큰 틀을 정해야 함.
  - 복잡도: 중간 (정책 결정 자체는 복잡도보다 결정의 문제. 실제 결제 연동은 3단계 별도 항목)

---

## 2단계 — 핵심 신규 기능

- [ ] **우클릭 메뉴에서 폴더 추가·이름변경·삭제 직접 처리**
  - 배경: 현재(v1)는 유튜브 페이지 우클릭 메뉴에 폴더 목록만 뜨고, 폴더를 새로 만들거나 이름을 바꾸거나 지우려면 별도의 "폴더 관리" 화면(새 탭)을 열어야 함. 산들이 실제로 우클릭 메뉴를 쓰다가 발견한 불편함.
  - 목표: 우클릭 메뉴 안에서 바로 새 폴더 만들기 / 이름변경 / 삭제까지 처리 → 페이지를 벗어날 필요 없음
  - 구현 메모: Chrome의 우클릭 메뉴(`contextMenus` API)는 글자를 직접 입력하는 칸을 지원하지 않아서, "새 폴더 만들기"를 누르면 유튜브 페이지 위에 작은 입력창(미니 팝업)을 띄우는 방식이 필요함. 이름변경·삭제도 같은 방식. 폴더/영상 추가·이름변경·삭제 로직 자체는 v1에도 이미 있어서(storage.js), 이번 작업은 주로 새로운 진입점(우클릭 메뉴 UI)을 추가하는 것이지 데이터 구조를 바꾸는 게 아님 → 1단계 결정과 무관하게 병행 가능.
  - 복잡도: 중간 (구조 변경 없음, UI 진입점 추가 위주)

- [ ] **유튜브 재생목록 일괄 가져오기**
  - 이유: 처음 쓸 때 폴더를 하나하나 채우는 수고를 줄여줌. 구조 변경 없이 추가 가능한 기능이라 먼저 처리.
  - 방식: YouTube Data API(키 필요, 50개/호출) 또는 키 없이 oEmbed 반복 호출
  - 복잡도: 중간

- [ ] **이어보기(재생 위치 기억) 기능**
  - 내용: 시청 중단 시점부터 이어서 재생
  - 주의: v1은 영상 클릭 시 유튜브로 새 탭을 여는 방식(`window.open`)이라 재생 위치를 알 수 없음. 이어보기를 구현하려면 영상을 앱 안에 내장 재생(유튜브 IFrame Player API)하는 방식으로 전환해야 하는데, 이건 v1 대비 꽤 큰 구조 변경. 2단계 안에서는 구조 변경이 걸려있어 가장 나중으로 배치.
  - 복잡도: 높음 (구조 변경 수반)

---

## 3단계 — 고급 기능 (핵심 안정화 후 권장)

- [ ] **모바일 자동 동기화 (구글 드라이브 또는 원드라이브)**
  - 이유: 기존 GAP-ANALYSIS 문서에서 가장 높은 우선순위로 평가됐던 항목이지만, OAuth 인증·충돌 해결 로직이 들어가 리스크가 커서 핵심 기능이 안정된 뒤 진행을 권장.
  - 참고: 구글 드라이브는 appDataFolder 방식으로 기존에 분석됨(무료·서버 불필요·OAuth 비민감 스코프). 원드라이브는 이번에 새로 언급되어 아직 기술 검토가 안 된 상태 — 착수 시 OneDrive Graph API 방식도 함께 조사 필요.
  - 복잡도: 높음

- [ ] **결제 연동 구현 (유료화 실장)**
  - 전제: 1단계 "유료화 정책 결정" 완료 후 착수
  - 이유: 크롬 확장은 자체 서버 없이 결제를 붙이기 까다로움. Chrome 자체 인앱결제 지원 여부는 시점에 따라 달라질 수 있어 착수 시점에 재확인 필요 — 확장 전용 결제 대행 서비스(예: ExtensionPay 등, Stripe 기반이라 별도 서버 코드 없이도 사용 가능한 종류가 있음) 조사부터 시작.
  - 필요 작업: 결제 서비스 선정 → 유료/무료 기능 잠금 로직 → 라이선스 상태 저장(1단계 저장소 백엔드에 반영) → 결제 실패/환불 처리
  - 복잡도: 높음 (외부 서비스 연동 + 검증 로직)

---

## 4단계 — 마무리 다듬기

- [ ] **폴더 아이콘 다양화 + 기본 아이콘으로 초기화**
  - 배경: 지금(v1)은 모든 폴더 아이콘이 노란 폴더 하나로 동일해서 폴더가 많아지면 구분이 안 됨(실사용 화면에서 확인됨).
  - 목표: 폴더마다 다른 아이콘을 고를 수 있는 메뉴 추가, 종류별(예: 음악/취미/공부/업무/생활 등 카테고리)로 구분해서 고르기 쉽게. 원하면 다시 기본 아이콘 하나로 되돌리는 "초기화" 기능도 포함.
  - 아이콘 소스: 저작권 문제 없는 오픈소스 아이콘셋에서 수집 필요 — 예: Material Symbols(Apache 2.0), Heroicons(MIT), Font Awesome Free(무료 라이선스 항목만) 등 라이선스가 명확히 무료·재배포 허용인 것만 사용. 실제 착수 시 라이선스 표기(출처) 문서화 필요.
  - 구현 메모: 폴더 데이터에 `icon` 필드 추가(현재 storage.js 데이터 모델에 없음 — 작은 추가라 1단계 저장소 백엔드 결정과 무관하게 진행 가능). 아이콘 없으면 기존 기본 폴더 아이콘 사용(하위 호환).
  - 복잡도: 낮음~중간

- [ ] 드래그로 순서 재배치 시 삽입선 표시 (현재 v1은 `Alt+↑/↓`로만 수동 정렬)
- [ ] 영상 `duration` 실제 수집 ('크기' 정렬 정밀화)
- [ ] 휴지통 보존기간 설정·자동 비우기
- [ ] 접근성(role/aria/키보드 포커스 링) 보강

---

## 다음에 결정할 것

- 유료화 정책의 구체적 내용(어떤 기능을 유료로 할지, 가격) — 1단계 항목으로 등록됨, 착수 시점에 산들과 확정 필요
