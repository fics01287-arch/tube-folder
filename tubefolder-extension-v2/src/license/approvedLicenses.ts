// 승인 기반 무료 라이선스 — 화이트리스트 (신설 2026-07-29, ROADMAP-CHECKLIST.md 3단계
// "승인 기반 무료 라이선스 구현", CLAUDE.md "유료화 대비 개발 원칙" 반영).
//
// 목적: 산들이 승인한 특정 사용자(지인 테스트·홍보용 배포 등)에게 결제 없이 무료로 제공하는 경로.
// (2026-07-30 Paddle 전환으로 결제 확인용 서버(Cloudflare Worker)가 별도로 생겼지만, 이 화이트리스트는
// 그와 무관하게 여전히 서버 조회 없이 빌드에 포함된 값을 클라이언트에서 직접 대조하는 방식으로 검증한다
// — 소수 대상에게 간단히 배포하는 용도라 별도 서버가 굳이 필요 없기 때문(검증 로직은 licenseEngine.ts의
// verifyLicenseKey 참고).
//
// 알려진 한계(결함 아님 — 서버 없는 방식의 근본적 트레이드오프): 이 배열은 빌드된 JS 번들에 그대로
// 포함되므로, 번들을 직접 열어보면 키·이메일 목록을 확인할 수 있다. 배포 대상이 산들이 직접 선정한
// 소수의 지인·홍보 대상이라는 낮은 위협 모델을 전제로 한 "간단한 확인 절차" 수준의 보호이지, 실제
// 결제(Paddle 웹훅 서버 검증)와 동등한 보안 수준은 아니다. 불특정 다수의 무단 사용을 막는 것이 목적이
// 아니라 "산들이 지정한 사람에게 결제 없이 간단히 제공"이 목적이므로 이 수준으로 충분.
//
// 새 승인 대상 추가 방법: 아래 배열에 { key, email } 한 줄을 추가하고 재빌드·재배포한다.
// key는 산들이 직접 정하는 임의 문자열(예: "TF-이름-0001") — 형식이 강제되지 않으며 문자 그대로
// 정확히 일치해야 통과한다(공백은 앞뒤로 트리밍). email은 대소문자 구분 없이 비교한다.
export interface ApprovedLicense {
  key: string;
  email: string;
  /** 산들이 누구에게·왜 발급했는지 남기는 메모(코드에만 남음, 검증 로직에는 쓰이지 않음) */
  note?: string;
}

export const APPROVED_LICENSES: ApprovedLicense[] = [
  // 산들 본인 개발자 테스트 계정 — 2026-09-07, 재생목록 가져오기 기능을 실제 계정으로 반복
  // 테스트하다 무료 티어 영상 한도(150개)에 걸려 이후 가져오기가 전부 조용히 실패하는 문제를
  // 발견한 것을 계기로 추가(대화 참고). 개발 중에는 이 한도에 계속 막히면 테스트 자체가
  // 불가능하므로, 산들 본인 계정만 화이트리스트로 무제한 처리한다 — 다른 사용자에게 배포되는
  // 빌드의 무료 티어 정책 자체는 그대로 유지.
  { key: 'TF-SANDEUL-DEV-0001', email: 'fics01287@gmail.com', note: '산들 본인 — 개발/테스트용' }
];
